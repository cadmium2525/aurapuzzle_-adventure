# テクニカルダンジョン用の敵スキル

複数敵を使う降臨ダンジョンでは各フロアの `enemies` に敵定義を並べる。`enemySkills.random: true` で通常行動候補を等確率抽選し、`buildUpBelow` でHPが指定%未満になった際の一度限りのビルドアップを設定する。キュウコの実例と分身体・ドロップ仕様は `RAID_DESIGN.md` を参照。

各スキルには専用モーションが付く。バインドは対象へ紫の霧、遅延は対象へ逆回転時計、コンボガードは防壁リング、形状指定は指定形の紋章、オーラバインドは該当マスの封印、時間短縮は縮む時計、時間固定は枠付き時計、先制は放射光、吸収は敵へ集まる渦、ビルドアップは上昇する矢印、根性は金色の星形を表示する。ガード・吸収・根性はダメージ判定時にも演出。端末の「視差効果を減らす」設定では動きを抑えて短時間表示する。

敵側の持続効果（攻撃上昇・根性・吸収・コンボガード・形状指定）は敵の右側に生成イラストのバッジで表示。タップで詳細を確認できる。ATK×2・HP1・CHAINの文字モーションは表示しない。バッジは効果失効・根性解除・フロア変更に追随する。

各フロア定義に `enemySkills` を追加する。既存ダンジョンには設定していないため従来通り通常攻撃する。

```js
enemySkills: {
  passives: [
    { type: 'resolve', threshold: 30 },
    { type: 'comboGuard', chains: 3 }
  ],
  preemptive: {
    attack: false,
    effects: [{ type: 'timeFixed', seconds: 5, turns: 2 }]
  },
  actions: [
    { attack: false, effects: [{ type: 'bind', count: 2, turns: 2 }] },
    { attack: true, effects: [{ type: 'buildUp' }] }
  ]
}
```

`passives` は出現時の常時効果。`preemptive` は初回を含むフロア出現直後に一度実行し、完了まで操作を受け付けない。`actions` は通常の敵行動カウントが0になったときに順番に実行し、末尾から先頭に戻る。省略時は通常攻撃。`attack: true` でスキルと攻撃を併用でき、ビルドアップはその行動の攻撃にも反映される。

| type | パラメータ | 効果 |
| --- | --- | --- |
| bind | count, turns | ランダムな重複なしの人数をバインド。攻撃・回復・スキル使用不可。リーダースキルと基礎ステータスは維持 |
| comboGuard | chains, turns（省略可） | 手番の最終チェイン数が指定値以下なら全ダメージ無効。同時消しは1チェイン |
| skillDelay | count, turns | ランダムな重複なしの人数の残りスキルターンを増加 |
| shapeGuard | aura, shape または cells, rotate, label, turns（省略可） | 指定色・形の同色連結グループをその手番に消していなければ全ダメージ無効 |
| auraBind | aura, turns | 指定色を消去対象から除外。移動・変換は可能 |
| timeReduce | seconds, turns | 操作時間を秒数分短縮。下限0.1秒 |
| timeFixed | seconds, turns | 操作時間を秒数に固定。リーダー・スキルの延長より優先。短縮と固定は後から付いた方で上書き |
| auraAbsorb | aura, turns | 指定色のダメージを敵回復へ変換。最大HPまで |
| buildUp | なし | 攻撃力2倍。重ねても4倍にならない。フロア終了まで |
| resolve | threshold | HPが指定パーセントより多い間、致死ダメージをHP1で耐える。指定値以下になれば永久解除 |

オーラ番号は火=0、水=1、木=2、癒=3、闇=4。

形状プリセットは `L`（5個）、`cross`（十字5個）、`square`（2×2）、`line`（直線4個）。`cells: [[0,0], ...]` で独自形状も定義可能。位置は不問、回転は標準で許可（`rotate: false` で禁止）、鏡像は不許可。同色の余分な連結がある場合は形状不一致。`label` で画面表示用の名前を指定できる。

ターン効果はプレイヤーの手番を完了した時に減る。敵行動で付いた直後には減らない。バインド・消去不可・時間変更はフロアをまたいで残り手番を引き継ぎ、敵の防御・攻撃上昇・根性は新フロアでリセット。スキルのみで倒した場合は手番消費に数えない（既存のフロア移動時クールダウン短縮は維持）。

ダメージ判定順はコンボ／形状ガード → 属性ごとの攻撃と吸収を合算 → 根性。直接ダメージスキルは使用者のオーラ属性、チェイン0・消去形状なしとして同じ判定を通す。根性は手番全体の合計ダメージに対して判定する。
