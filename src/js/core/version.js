/* =========================================================
 * version.js — アプリの版
 *
 * index.html の <meta name="app-version"> と必ず同じ値にする。
 * サービスワーカーはHTMLとJSを別々に取りに行くので、通信が不安定だと
 * 「古いHTML + 新しいJS」の組み合わせで起動してしまうことがある。
 * そうなると main.js が無い要素を触って落ち、ローディングが0%のまま
 * 固まる。この2つを突き合わせて食い違いを検出する。
 *
 * 更新するときは3か所そろえること:
 *   1. ここ
 *   2. index.html の <meta name="app-version">
 *   3. sw.js の CACHE_NAME
 * =======================================================*/
export const APP_VERSION = '44';
